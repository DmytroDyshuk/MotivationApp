<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:button="@drawable/checkbox_selector" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/mcv_sound_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginStart="12dp">

        <ImageView
            android:id="@+id/iv_sound_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/content_description_sound_image"
            android:scaleType="centerCrop" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        tools:text="Sound name"
        android:textSize="16sp"
        android:fontFamily="@font/dm_sans_medium"
        android:textColor="@color/pickled_bluewood"
        android:layout_marginStart="12dp" />

    <ImageView
        android:id="@+id/iv_playback_sound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/vector_playback_first_icon"
        android:contentDescription="@string/content_description_sound_playback_image"
        android:layout_marginEnd="18dp" />

</LinearLayout>